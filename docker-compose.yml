services:
  db:
    image: postgres:15
    env_file: apps/back/.env
    ports:
      - '5432:5432'
    volumes:
      - pararara:/var/lib/postgresql/data

  backend:
    build: ./apps/back
    env_file: ./apps/back/.env
    ports:
      - '8080:8080'
    volumes:
      - ./apps/back:/app
      - /app/node_modules
    depends_on:
      - db

  frontend:
    build: ./apps/front
    env_file: ./apps/front/.env
    ports:
      - '8000:5173'
    volumes:
      - ./apps/front:/app
      - /app/node_modules
    depends_on:
      - backend

volumes:
  pararara:
